# Лабораторная работа №5

## Цель

Разработать функцию для генерации бинарного дерева с заданными параметрами и реализовать визуализацию структуры дерева в виде вложенного словаря.

---

## Задание

Написать функцию `gen_bin_tree`, которая принимает следующие параметры:
- `root` — значение корня дерева (по моему варианту: `1`)
- `height` — высота дерева (количество поколений) (по моему варианту: `5`)
- `left_branch` — функция для генерации левого потомка (по моему варианту: `x * 2`)
- `right_branch` — функция для генерации правого потомка (по моему варианту: `x + 3`)

Функция должна возвращать бинарное дерево в виде вложенного словаря, где каждый узел представлен как `{значение: [левый_потомок, правый_потомок]}`.

---

## Код программы

```python
def gen_bin_tree(root, height, left_branch=lambda x: x * 2, right_branch=lambda x: x + 3) -> dict[str, any]:
    '''
    :param height: Задаёт количество "поколений" дерева, включая "поколение" корня
    :param root: значение корня
    :param left_branch: значение для левого "наследника"
    :param right_branch: значение для правого "наследника"
    :return: готовое дерево
    '''
    # рассматриваем отдельный случай, когда количество поколений равно 1
    if height == 1:
        return {f'{root}': []}
    
    # генерация значений всех поколений дерева
    tree = [[root]]
    for i in range(height-1):
        last_level = tree[-1]
        new_level = []
        for i in last_level:
            tree_level_left = left_branch(i)
            tree_level_right = right_branch(i)
            new_level += [tree_level_left, tree_level_right]
        tree.append(new_level)

    # функция генерации словаря
    def dict_gen(key, equal) -> dict[any, any]:
        return {f'{key}': equal}

    # Обратная сортировка дерева для более удобной реализации
    tree.sort(reverse=True, key=len)

    # Создание "словарного" вида последних элементов
    end_of_tree = []
    for i in range(len(tree[0])):
        end_of_tree.append(dict_gen(tree[0][i], []))
    tree[0] = end_of_tree

    # занесение всех элементов поколений в конструкцию дерева "словарно-списочного" вида
    for i in range(1, len(tree)):
        new_level = []
        for j in range(len(tree[i])):
            new_level += [dict_gen(tree[i][j], [tree[i-1][2*j], tree[i-1][2*j+1]])]
        tree[i] = new_level

    # создание готового дерева, берется корень и остальная сформированная конструкция дерева
    ready_tree = dict_gen(root, tree[-2])
    
    return ready_tree

print(gen_bin_tree(1, 5))
```

## Вывод

В ходе выполнения лабораторной работы была реализована функция `gen_bin_tree`, которая генерирует бинарное дерево заданной высоты.

**Основные результаты:**

<ol>
  <li>
    <strong>Функция принимает параметры:</strong>
    <ul>
      <li><code>root</code> — значение корня дерева</li>
      <li><code>height</code> — высота дерева (количество поколений)</li>
      <li><code>left_branch</code> и <code>right_branch</code> — функции для вычисления значений потомков</li>
    </ul>
  </li>
  
  <li>
    <strong>Особенности реализации:</strong>
    <ul>
      <li>Функции-аргументы по умолчанию: <code>x * 2</code> для левой ветви и <code>x + 3</code> для правой</li>
      <li>Возвращаемое значение — вложенный словарь вида <code>{значение: [левый_потомок, правый_потомок]}</code></li>
      <li>Обработка граничного случая при <code>height = 1</code></li>
    </ul>
  </li>
  
  <li>
    <strong>Принцип работы алгоритма:</strong>
    <ul>
      <li>Сначала генерируются значения всех узлов по уровням</li>
      <li>Затем выполняется преобразование линейной структуры во вложенные словари</li>
      <li>Обратная сортировка уровней упрощает процесс вложения</li>
    </ul>
  </li>
  
  <li>
    <strong>Гибкость использования:</strong>
    <ul>
      <li>Возможность подставить любые функции для генерации потомков</li>
      <li>Функция может применяться для моделирования различных древовидных структур</li>
    </ul>
  </li>
</ol>

Таким образом, реализована универсальная функция для генерации бинарного дерева с возможностью кастомизации правил ветвления.
## Дополнительно

 Ссылка на репозиторий в GitHub:[https://github.com/CrassusPiscis/ITMO_Python/tree/master/Lab_5](https://github.com/CrassusPiscis/ITMO_Python/tree/master/Lab_5)